# Where the Socket.IO server lives.
# Leave this blank in development: the Vite dev-server proxy (vite.config.js)
# forwards /socket.io and /api to localhost:5000 for you.
# In production set it to your deployed backend, e.g. https://api.example.com
VITE_SERVER_URL=
